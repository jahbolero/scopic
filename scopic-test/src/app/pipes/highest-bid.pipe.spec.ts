import { HighestBidPipe } from './highest-bid.pipe';

describe('HighestBidPipe', () => {
  it('create an instance', () => {
    const pipe = new HighestBidPipe();
    expect(pipe).toBeTruthy();
  });
});
