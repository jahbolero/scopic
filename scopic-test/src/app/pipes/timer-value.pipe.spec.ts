import { TimerValuePipe } from './timer-value.pipe';

describe('TimerValuePipe', () => {
  it('create an instance', () => {
    const pipe = new TimerValuePipe();
    expect(pipe).toBeTruthy();
  });
});
